<h1>Академгрупи</h1>

<div id="groups">
  <table>
    <tr>
      <th>№</th><th>Шифр академгрупи</th><th></th>
    </tr>
    <% index = 0 %>
    <% @groups.each do |group| %>
      <% index += 1 %>
      <tr>
        <td class="align-right"><%= index %></td>
        <td><%= group.group %></td>
        <td><%= link_to "Подробиці", group %></td>
      </tr>
    <% end %>
  </table>
</div>

<p>
  <%= link_to "Додати академгрупу", new_group_path %> |
  <%= link_to 'Імпорт із текстового файлу', new_import_groups_path %>
</p>