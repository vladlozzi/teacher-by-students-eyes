<h1>Критерії для оцінювання викладачів</h1>

<div id="criteria">
  <table>
    <tr>
      <th>№</th><th>Назва критерію</th><th></th>
    </tr>
    <% index = 0 %>
    <% @criteria.each do |criterium| %>
      <% index += 1 %>
      <tr>
        <td class="align-right"><%= index %></td>
        <td><%= criterium.name %></td>
        <td><%= link_to "Подробиці", criterium %></td>
      </tr>
    <% end %>
  </table>
</div>

<p>
  <%= link_to "Додати критерій", new_criterium_path %> |
  <%= link_to 'Імпорт із текстового файлу', new_import_criteria_path %>
</p>
