<h1>Студенти/студентки</h1>

<div id="students">
  <table>
    <tr>
      <th>№</th><th>Прізвище, ім'я та по батькові (за наявності)</th><th></th>
    </tr>
    <% index = 0 %>
    <% @students.each do |student| %>
      <% index += 1 %>
      <tr>
        <td class="align-right"><%= index %></td>
        <td><%= student.full_name %></td>
        <td><%= link_to "Подробиці", student %></td>
      </tr>
    <% end %>
  </table>
</div>

<p>
  <%= link_to "Додати студента/студентку", new_student_path %> |
  <%= link_to 'Імпорт із текстового файлу', new_import_students_path %>
</p>
